# Neptune 4 Max USB-C toolhead overrides, verified by the downstream v2.3
# hardware work. PB10 drives the part light as a regular output on this
# toolhead; it must not simultaneously be declared as a neopixel.

part_light_pin=THR:PB10
toolhead_extra_sections=
